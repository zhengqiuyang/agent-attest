{
  "name": "agent-attest",
  "version": "0.2.0",
  "description": "Signed chain-of-custody for AI coding agents — attest what the agent did, gate protected paths in CI, changelog with provenance",
  "type": "module",
  "license": "MIT",
  "author": "agent-attest contributors",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zhengqiuyang/agent-attest.git"
  },
  "bugs": "https://github.com/zhengqiuyang/agent-attest/issues",
  "keywords": [
    "ai-agents",
    "security",
    "supply-chain",
    "in-toto",
    "compliance",
    "attestation"
  ],
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "agent-attest": "bin/agent-attest.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node run-tests.mjs",
    "demo": "node scripts/demo.mjs",
    "prepublishOnly": "npm run build && npm test"
  },
  "dependencies": {
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "typescript": "^5.5.0"
  },
  "files": [
    "dist",
    "bin",
    "README.md",
    "LICENSE"
  ]
}
